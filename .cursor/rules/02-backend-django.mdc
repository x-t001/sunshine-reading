---
description: Django 后端架构与服务分层约束
alwaysApply: true
---

# 02 后端 Django 规则

## 目的
- 定义后端分层、模块职责与 API 实现边界。

## 输入
- 领域用例
- API 契约
- 权限矩阵

## 步骤
1. 仅使用规划的后端结构：
   - `backend/apps/<domain>/models.py`：实体模型
   - `backend/apps/<domain>/services.py`：业务逻辑
   - `backend/apps/<domain>/serializers.py`：输入输出结构
   - `backend/apps/<domain>/views.py`：HTTP 层
2. 强制遵循 `views -> services -> models` 分层，视图层不得包含业务逻辑。
3. 请求数据进入服务层前，必须通过序列化器或结构定义完成校验。
4. 所有响应必须使用 `03-api-contract.mdc` 定义的统一 API 封装。
5. 当前阶段禁止执行 Django 脚手架或初始化命令。

## 产出
- 职责边界清晰的后端蓝图。

## 检查清单
- [ ] 模块结构符合契约。
- [ ] 视图、服务与模型分层正确。
- [ ] 响应封装统一。
- [ ] 未执行 Django 初始化。
