---
description: 强制 API 响应封装与契约规范
alwaysApply: true
---

# 03 API 契约规则

## 目的
- 强制前后端使用统一且确定的 API 返回格式。

## 输入
- 接口定义
- 请求结构
- 业务结果

## 步骤
1. 接口统一采用 `/api/v1/<resource>`，并明确请求方法与权限。
2. 请求体、查询参数和路径参数必须说明类型、是否必填、默认值与约束。
3. 使用统一响应封装：
   - 成功：
     `{ "code": 0, "message": "ok", "data": <payload>, "request_id": "<id>" }`
   - 失败：
     `{ "code": <non_zero>, "message": "<error>", "errors": [ { "field": "<name>", "reason": "<msg>" } ], "request_id": "<id>" }`
4. 列表接口必须在 `data` 内返回分页对象：
   `{ "page": 1, "page_size": 20, "total": 100, "items": [] }`
5. 错误码必须稳定并文档化，禁止将同一个错误码用于无关错误。

## 产出
- 稳定且机器可读的 API 契约。

## 检查清单
- [ ] URL 已版本化。
- [ ] 响应封装严格符合规则。
- [ ] 分页结构已标准化。
- [ ] 错误码确定且不冲突。
