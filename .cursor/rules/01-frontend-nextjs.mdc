---
description: 前端架构与目录分层约束
alwaysApply: true
---

# 01 前端 Next.js 规则

## 目的
- 为未来前端实现定义严格的分层与目录契约。

## 输入
- 页面需求
- API 契约
- UI 风格规则

## 步骤
1. 仅使用规划的前端结构：
   - `frontend/src/app`：路由
   - `frontend/src/features`：功能模块
   - `frontend/src/components`：通用 UI 组件
   - `frontend/src/lib`：API 客户端与工具库
2. 强制遵循 `app -> features -> components/lib` 分层，禁止反向依赖。
3. 数据获取必须放在功能层或应用层，共享展示组件不得直接请求数据。
4. 每个页面必须定义加载、空态、错误态及权限兜底。
5. 当前阶段禁止执行 Next.js 脚手架或初始化命令。

## 产出
- 可执行的前端分层蓝图。

## 检查清单
- [ ] 已遵循目录契约。
- [ ] 分层依赖方向正确。
- [ ] 状态处理完整。
- [ ] 未执行 Next.js 初始化。
