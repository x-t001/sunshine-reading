---
description: 权限模型、输入安全与审计约束
alwaysApply: true
---

# 06 安全与权限规则

## 目的
- 对所有接口强制执行基于角色的访问控制与安全数据处理。

## 输入
- 角色
- 资源操作
- 威胁假设

## 步骤
1. 使用 `reader`、`author`、`admin` 作为角色基线，未定义角色默认拒绝访问。
2. 为每个接口建立“动作 -> 允许角色 -> 校验规则”的权限矩阵。
3. 校验并清洗所有外部输入，拒绝危险的 HTML 或脚本内容。
4. 在日志和链路追踪中脱敏 `password`、`token`、`phone`、`email` 等敏感字段。
5. 对创建、更新、删除、发布和权限变更操作记录审计事件。

## 产出
- 可直接落地的权限与安全执行基线。

## 检查清单
- [ ] 权限矩阵完整。
- [ ] 已执行输入校验与清洗。
- [ ] 已启用敏感数据脱敏。
- [ ] 已定义关键操作的审计事件。
