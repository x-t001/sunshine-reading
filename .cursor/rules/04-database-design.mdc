---
description: 数据库结构命名、约束与迁移策略
alwaysApply: true
---

# 04 数据库设计规则

## 目的
- 为小说阅读领域数据建立一致的结构设计规范。

## 输入
- 实体清单
- 访问模式
- 完整性约束

## 步骤
1. 表名和字段名必须使用英文 `snake_case`；主键统一为 `id`，类型使用 `bigint` 或 `uuid`。
2. 核心表应包含 `created_at`、`updated_at`，并按需增加软删除字段 `deleted_at`。
3. 为业务唯一标识定义唯一约束，例如 `book_slug`、`chapter_no`。
4. 为高频筛选和排序字段增加索引，避免对写入密集字段过度建立索引。
5. 每次数据库结构变更都必须包含正向迁移方案和回滚风险评估。

## 产出
- 迁移安全且查询高效的关系型结构。

## 检查清单
- [ ] 命名与类型已标准化。
- [ ] 唯一约束与索引有明确依据。
- [ ] 审计与软删除策略明确。
- [ ] 已评估迁移回滚风险。
